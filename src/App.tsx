import Translator from '@/pages/Translator'

// Simple English ⇄ Spanish (Latin American) vocabulary translator.
// The earlier "Mi Profesor" tutor pages still live under src/pages/ and
// can be re-wired with a router if they're needed again.
export default function App() {
  return <Translator />
}
